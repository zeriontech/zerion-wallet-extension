/**
 * Half of `PageTop`. The flow's first line of copy is a caption rather than a
 * heading, so the standard 16px leaves it stranded below the navigation bar.
 */
export const DEPOSIT_PAGE_TOP = 8;

/** Fixed copy: shown immediately, including while the form is still loading. */
export const DEPOSIT_SUBTITLE =
  'Use credit/debit card, or bank transfer to buy crypto';
